cd /home/deploy/my-worker

# Initialize git if not already done
git init

# Add your GitHub repository as origin
git remote add origin https://github.com/747imhotep/747civilbarb.git



# Fetch the latest changes
git fetch origin

# Pull the main branch
git pull origin main --allow-unrelated-histories


If you get a merge conflict, run:
git reset --hard origin/main


sudo nginx -t
sudo systemctl reload nginx
pm2 restart my-worker

================================================
GitHub to VPS
git pull origin master --allow-unrelated-histories --no-rebase


VPS to GitHub
git push origin master

